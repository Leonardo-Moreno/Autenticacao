<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leonardomlmendes.autenticacao">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splashActivity">

        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".SBActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdicionarActivity" />
        <activity android:name=".BancoActivity" />
        <activity
            android:name=".IdeiasActivity"
            android:label="@string/title_activity_ideias"
            android:theme="@style/AppTheme" />
        <activity android:name=".LinksActivity" />
        <activity android:name=".WifiActivity"></activity>
    </application>

</manifest>